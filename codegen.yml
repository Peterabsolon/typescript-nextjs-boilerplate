overwrite: true
schema: './schema.graphql'
documents: null
generates:
  src/generated/index.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'

    config:
      skipTypename: true
      avoidOptionals: true
      declarationKind: 'interface'
